package de.ble1st.files.ui.viewer

/** Welcher eigene Betrachter für einen Tap auf eine Datei zuständig ist — s.
 * [de.ble1st.files.nav.FilesNavHost] Dispatch-Logik. */
enum class ViewerCategory { IMAGE, VIDEO, TEXT }
